# Sprite assets

Place character PNG sheets and manifests in this folder. To use the supplied
launcher artwork for the Windows executable, save it here as
`sprite_engine_icon.png`, then run `Build Sprite Engine EXE.bat`.